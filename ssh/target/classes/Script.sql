--<ScriptOptions statementTerminator=";"/>

CREATE TABLE T_MP_USER_ROLE (
		ID CHAR(32) NOT NULL,
		USER_ID NUMBER(10 , 0) NOT NULL,
		ROLE_ID CHAR(4) NOT NULL
	);

CREATE UNIQUE INDEX USER_ROLE_ID ON T_MP_USER_ROLE (ID ASC);

ALTER TABLE T_MP_USER_ROLE ADD CONSTRAINT USER_ROLE_ID PRIMARY KEY (ID);

